apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: uniia-landing
spec:
  rules:
  - host: test.uniia.com.ua
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: uniia-landing
            port:
              number: 80
  tls:
  - hosts:
    - test.uniia.com.ua
    secretName: uniia-test-tls-cert 