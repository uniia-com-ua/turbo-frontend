apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../resources

namespace: web-landing-test

images:
- name: ghcr.io/YOUR_ORG/uniia-landing
  newTag: latest-rc

patchesStrategicMerge:
- patch-deployment.yaml
- patch-ingress.yaml 